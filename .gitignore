# .NET
bin/
obj/
*.user
*.suo
.vs/
src/**/bin/
src/**/obj/

# 前端
node_modules/
frontend/dist/
frontend/node_modules/

# 日志
*.log
build_log.txt
api_stdout.log
api_stderr.log
vite_stdout.log
vite_stderr.log
docker-build.log
docker-build-api.log
docker-up.log

# IDE
.idea/
.vscode/
*.swp

# 环境变量
.env
.env.local

# 迭代服务配置（由「改进建议 → 迭代服务配置」页写入，**里面有管理员密钥**，绝不入库）
# 两处都要忽略：容器里是 <仓库>/config（编排挂载），`dotnet run` 时是 <项目目录>/config
/config/
src/QiaoMES.Api/config/

# AI 自迭代服务的本地数据（SQLite 里有建议/计划/审阅留痕，还可能有 git 凭据脚本，绝不入库）
/ai-iteration/
